# 本地开发时使用本地 @talon-sandbox/react 源码（实时 HMR），不读 npm 装的版本。
# 复制本文件为 .env.local 并取消下一行注释即可启用；CI / 生产构建 不应设置此变量。
# 也可以在命令行临时启用：USE_LOCAL_UI=1 npm run dev
# USE_LOCAL_UI=1

# 本地开发若需直连远程 BFF（例如 staging）：
# VITE_PLAYGROUND_API_BASE=https://playground.sandbox.talon.net.cn/playground/api
#
# 注意：前端不再持有 API Key，Authorization 由后端 BFF 层注入，此处无需配置 key。
